apiVersion: v1
kind: Service
metadata:
  name: site
  labels:
    app: site
    {{- include "marketing-site.labels" . | nindent 4 }}
spec:
  type: LoadBalancer
  selector:
    app: site
  ports:
    - port: 80
      targetPort: 8080
