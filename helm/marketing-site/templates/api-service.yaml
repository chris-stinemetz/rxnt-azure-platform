apiVersion: v1
kind: Service
metadata:
  name: api
  labels:
    app: api
    {{- include "marketing-site.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    app: api
  ports:
    - port: 80
      targetPort: 8080
